## Directory summary
Hydra configuration for standalone preflop all-in equity model training.

### Source files
- `config.yaml`: Default Hydra config for standalone all-in equity training.

### Subdirectories
There are no child source directories.
