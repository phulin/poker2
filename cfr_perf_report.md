# CFR Main-Path Performance Report

## Benchmark Harness
- Added `scripts/bench_cfr_main_path.py`.
- Runs realistic `RebelCFRTrainer.train_step` profiles with wandb/trueskill disabled.
- Applies requested model settings: `hidden_dim=256`, `range_hidden_dim=128`, `ffn_dim=512`, `num_hidden_layers=3`, `num_value_layers=1`, `num_policy_layers=1`.
- Pauses matching `train_rebel` processes before benchmark sections and resumes them afterward.
- Writes structured JSON output under `outputs/`.

## Baseline
Best source profile: `outputs/cfr_main_path_source_realistic_v2.json`.

- Active steady-state wall: `1.374s/step`.
- Largest regions:
  - Data generation: `718ms` inclusive CUDA-tagged.
  - CFR evaluate: `447ms`.
  - Supervised update: `146ms`.
  - Subgame init: `132ms`.
  - Metrics: `121ms`.
  - Replay buffer sampling: `248ms` CPU across value/policy sample tags.
  - Replay buffer add: `78ms` across value/policy add tags.

Expanded microbenchmarks: `outputs/cfr_main_path_micro_realistic_v2.json`.

## Main Findings
The next 2x is probably not mostly inside individual CFR iteration kernels. CUDA graph replay makes the visible per-iteration work small in the source profile. The larger costs are around the CFR loop and training loop: replay buffer CPU sampling/transfers, subgame setup/graph capture, metrics, and supervised-update batching.

## Recommended Work
1. Add a replay-buffer `sample_to_device` path with pinned CPU staging and preallocated GPU batch tensors.
2. Add a faster replay-buffer add path for GPU-produced training data, using pinned CPU destinations or asynchronous staging.
3. Reuse fused sparse evaluator buffers and bucket CUDA graphs by static tree shape/regime to avoid per-subgame graph capture/allocation work.
4. Move heavy metrics/aggression analysis to a lower cadence for training-fast runs.
5. Combine the two value forwards in `_supervise` into one doubled-batch value forward, and use `zero_grad(set_to_none=True)`.

## 2x Estimate
Baseline is `1.374s/step`; a 2x target is `~0.687s/step`.

Plausible output-preserving savings:
- Replay sample/add/transfer staging: `250-350ms`.
- CFR graph/subgame reuse: `250-350ms`.
- Lower-cadence metrics: `90-120ms`.
- Supervised update cleanup: `20-50ms`.

That totals `610-870ms`, which puts the loop in the `0.50-0.76s/step` range if the larger changes land.

## Caveats
- Source-profile component CUDA times are inclusive and nested; do not sum them.
- CUDA graph replay hides most per-iteration internals from Python record-function ranges.
- Microbench buffer-sample CUDA-event timings should be read as elapsed wall latency because the sampled tensors live on CPU.

## Latest Progress
- GPU replay buffers remove the CPU replay gather and transfer path in the ReBeL CFR config while keeping CPU as the dataclass default. Microbenchmarks improved value/policy sample from `23.26ms`/`36.48ms` on CPU replay to `0.36ms`/`0.48ms` on CUDA replay.
- Cached aggression hand groups and faster suit permutation reduce metrics/permutation overhead. `batch_permute` improved from `2.55ms` to `0.65ms`.
- Best clean one-step source run after these changes is `outputs/cfr_main_path_source_replay_device_cuda_fast_permute.json` at `0.703s/step`, about `1.95x` faster than the `1.374s/step` baseline. The strict 2x target is `~0.687s/step`, so the remaining gap is small but not conclusively closed.
- Added a Triton env row-gather fast path for sparse subgame expansion. On `outputs/spots.pt` with 1024 roots/street and depth 4, `construct_subgame` improved from `105.0/95.1/89.7/103.0ms` to `89.8/78.5/75.7/88.1ms` for preflop/flop/turn/river respectively. The clean source run `outputs/cfr_main_path_source_triton_env_gather_runtime_n.json` measured `0.703s/step`, with `cfr_init_subgame` at `129.19ms` vs `132.83ms` before.
- Added focused `spots.pt` microbenchmarks for CFR iterator, warm-start, and subgame initialization components. The subgame-init benchmark (`outputs/cfr_init_spots_uniform_parent_index.json`) found the init-time `uniform_policy` denominator expansion can use the already-built `parent_index` instead of another `repeat_interleave`, improving that component from `0.141ms` to `0.058ms` CUDA on average while asserting equality with the old formula.
- After the parent-index denominator change, `outputs/cfr_main_path_source_uniform_parent_index.json` measured `0.665s/step` on a realistic source run, crossing the strict `~0.687s/step` 2x target versus the `1.374s/step` baseline. In that run, `cfr_init_subgame` was `120.70ms/step`.
- Added an unfused mode to the balanced `spots.pt` iterator benchmark for direct core-loop progress checks. In matching short runs, `outputs/cfr_iterator_spots_unfused_baseline_ab4.json` averaged `70.76ms/iter` across pre/post DCFR segments, while `outputs/cfr_iterator_spots_fused_current_ab4.json` averaged `17.51ms/iter`, a `4.04x` speedup (`75.3%` lower wall time) on the requested `spots.pt` roots. The longer current run with BetterFFN fixed hand-index buffers, `outputs/cfr_iterator_spots_betterffn_hand_index_buffers_ab12.json`, averaged `14.52ms/iter`.
- BetterFFN now stores fixed hand rank/suit indices as non-persistent buffers. The targeted hand-embedding microbenchmark (`outputs/betterffn_hand_embedding_index_buffers_micro.json`) showed parity with the old formula and a small `~1.3%` CUDA reduction for that substep; the `spots.pt` iterator run above also showed a small model-forward reduction (`~4.92ms` vs prior `~4.99ms` CUDA per iteration segment).
- Corrected core-loop target is fused-to-fused, not unfused-to-fused: `outputs/cfr_iterator_spots_head_default_ab12.json` is the fused baseline at `15.01ms/iter`, so the 33% wall-clock reduction target is `<=10.0ms/iter`.
- BetterFFN exposes `static_feature_base`, and the fused evaluator caches that static board/street/context contribution per subgame while still using a compiled helper for compiled models. `outputs/cfr_iterator_spots_static_base_cache_compiled_ab12.json` averaged `13.95ms/iter`; `cfr_model_fwd` fell from `~4.92ms` to `~4.39ms` CUDA per iteration segment. This is a `7.1%` wall-clock reduction from the fused baseline, so the fused-to-fused target is still not met.
- The model-value writeback Triton launcher now uses 4 warps for the default no-mix/no-zero-sum writeback path while retaining 8 warps for reduction-heavy paths. `outputs/cfr_iterator_spots_writeback_4warps_ab12.json` averaged `13.65ms/iter`; a repeat with 20 active iterations (`outputs/cfr_iterator_spots_writeback_4warps_ab20.json`) averaged `13.21ms/iter`. The comparable `ab12` result is a `9.1%` wall-clock reduction from the fused baseline, still short of the `<=10.0ms/iter` target.
- A broader source-profile check after the static-base and writeback changes, `outputs/cfr_main_path_source_static_base_writeback.json`, measured `0.675s/step` with `cfr_iter` at `16.33ms/step` across the profiled train step.
- With `cfr_avg=false` and final-policy value targets, average-policy reach propagation is now deferred until the final average beliefs are needed. `outputs/cfr_iterator_spots_defer_avg_reach_ab12.json` averaged `13.37ms/iter`, and the longer `outputs/cfr_iterator_spots_defer_avg_reach_ab20.json` averaged `12.34ms/iter`. The comparable `ab12` result is a `10.9%` wall-clock reduction from the fused baseline; the target remains `<=10.0ms/iter`.
- The same final-policy path now also defers average-policy normalization itself, accumulating only numerator/denominator during CFR iterations and materializing `policy_probs_avg` once at finalization. `outputs/cfr_iterator_spots_defer_avg_policy_ab12.json` averaged `12.81ms/iter`; the longer `outputs/cfr_iterator_spots_defer_avg_policy_ab20.json` averaged `11.91ms/iter`. The comparable `ab12` result is a `14.6%` wall-clock reduction from the fused baseline; the target remains `<=10.0ms/iter`.
- The deferred average-policy accumulation path now uses 1024-hand Triton blocks. `outputs/cfr_iterator_spots_defer_avg_policy_block1024_ab12.json` averaged `12.34ms/iter`; `outputs/cfr_iterator_spots_defer_avg_policy_block1024_ab20.json` averaged `11.81ms/iter`. The comparable `ab12` result is a `17.8%` wall-clock reduction from the fused baseline; the target remains `<=10.0ms/iter`.
- A broader source-profile check after deferring average-policy finalization, `outputs/cfr_main_path_source_defer_avg_policy.json`, measured `0.652s/step`; `cfr_evaluate` was `410.69ms/step` and `cfr_iter` was `15.18ms/step`.
- `torch.compile(mode="max-autotune")` is available as an opt-in fused evaluator setting via `P2_FUSED_COMPILE_MODE=max-autotune`. It improved the iterator microbench slightly (`outputs/cfr_iterator_spots_max_autotune_ab20.json` averaged `11.60ms/iter`) but regressed the broader source profile (`outputs/cfr_main_path_source_max_autotune.json` measured `0.670s/step` vs `0.652s/step`), so the default remains standard `torch.compile(dynamic=True)`.
- The final-policy/deferred-average path now fuses per-depth reach propagation, belief normalization, and average-policy numerator/denominator accumulation. `outputs/cfr_iterator_spots_fused_reach_beliefs_avg_8warps_ab12.json` averaged `11.96ms/iter`; the longer `outputs/cfr_iterator_spots_fused_reach_beliefs_avg_8warps_ab20.json` averaged `11.78ms/iter`. The comparable `ab12` result is a `20.3%` wall-clock reduction from the `15.01ms/iter` fused baseline, still short of the `<=10.0ms/iter` target. The broader source-profile check `outputs/cfr_main_path_source_fused_reach_beliefs_avg.json` measured `0.651s/step`.
- The stat-free fused reach/belief/average path now keeps final-depth `self_reach` register-local instead of storing it: leaf reach has no descendants, and `_record_stats` only reads non-leaf reach. `outputs/cfr_iterator_spots_skip_leaf_reach_store_ab12.json` averaged `11.72ms/iter`; `outputs/cfr_iterator_spots_skip_leaf_reach_store_ab20.json` averaged `11.36ms/iter`. The comparable `ab12` result is a `21.9%` wall-clock reduction from the fused baseline, still short of the `<=10.0ms/iter` target. The broader source-profile check `outputs/cfr_main_path_source_skip_leaf_reach_store.json` measured `0.626s/step`.
- The fused evaluator now gathers model-leaf and showdown beliefs with one cached concatenated index tensor in the static-feature-cache path. `outputs/cfr_iterator_spots_combined_leaf_belief_gather_ab12.json` averaged `12.50ms/iter`, but the longer run `outputs/cfr_iterator_spots_combined_leaf_belief_gather_ab20.json` averaged `11.24ms/iter` with both pre/post delay segments below the prior `ab20` best. This is a `25.1%` wall-clock reduction from the fused baseline, still short of the `<=10.0ms/iter` target.
- `mean_regret_bound` logging and the now-dead `regret_weight_sums` tensor were removed from dense, sparse, and fused CFR paths. This eliminates a stats-time regret-bound reduction and one hot-loop tensor load/store stream in the fused DCFR update. `outputs/cfr_iterator_spots_no_regret_weight_sums_ab20.json` averaged `11.02ms/iter` (`11.475ms` pre-delay, `10.568ms` post-delay), with `cfr_iteration` down to `8.835ms`. This is a `26.6%` wall-clock reduction from the fused baseline, still short of the `<=10.0ms/iter` target.
- The stat-free fused reach/belief/average path now uses depth-local scratch buffers for reach instead of materializing non-observable `self_reach` rows across the full tree. A direct A/B in the current code measured `outputs/cfr_iterator_spots_scratch_reach_disabled_ab20.json` at `11.25ms/iter`, while `outputs/cfr_iterator_spots_scratch_reach_beliefs_avg_ab20.json` averaged `10.96ms/iter` (`11.338ms` pre-delay, `10.573ms` post-delay), with `cfr_iteration` at `8.820ms`. This is a `27.0%` wall-clock reduction from the fused baseline, still short of the `<=10.0ms/iter` target.
- `compute_expected_values` now reuses evaluator-owned actor-belief and marginal-policy buffers instead of allocating those large temporaries every iteration. `outputs/cfr_iterator_spots_ev_temp_buffers_ab20.json` averaged `10.66ms/iter` (`10.894ms` pre-delay, `10.421ms` post-delay), with `compute_expected_values` at `1.762ms` and `cfr_iteration` at `8.822ms`. This is a `29.0%` wall-clock reduction from the fused baseline, still short of the `<=10.0ms/iter` target.
- Regret source-weight setup now gathers opponent beliefs with a Triton row selector instead of PyTorch advanced indexing with a fresh `arange(top)`. The confirmation run `outputs/cfr_iterator_spots_regret_opp_select_triton_repeat_ab20.json` averaged `10.56ms/iter` (`10.813ms` pre-delay, `10.309ms` post-delay), with `cfr_iteration` at `8.708ms`. This is a `29.6%` wall-clock reduction from the fused baseline, still short of the `<=10.0ms/iter` target.
- The spots iterator harness now reports a direct `regret_src_weights` component timer. `outputs/cfr_iterator_spots_component_regret_src_weights_ab8.json` measured that subcomponent at `0.652ms` CUDA, giving a focused target inside `cfr_iteration`.
- The fused evaluator now removes padded duplicate showdown rows before hand-rank precompute and showdown EV graph capture. On the balanced spots benchmark this reduced `showdown_indices` from `1024` padded rows to `724` actual rows. `outputs/cfr_iterator_spots_unpadded_showdown_repeat_ab20.json` averaged `10.26ms/iter` (`10.548ms` pre-delay, `9.980ms` post-delay), with `set_leaf_values` down to `3.225ms` and `cfr_leaf_showdown` down to `~0.255ms`. This is a `31.6%` wall-clock reduction from the fused baseline, still just short of the `<=10.0ms/iter` target.
- The spots iterator harness now also reports the sparse `policy_sample_update` copy that runs at the top of `cfr_iteration`. `outputs/cfr_iterator_spots_component_policy_sample_update_ab8.json` measured it at only `0.059ms` CUDA, so it is not a meaningful remaining speed target.
- Added focused CUDA-event timers for `set_leaf_values` subcomponents. `outputs/cfr_iterator_spots_component_leaf_parts_ab8.json` measured `leaf_model_forward` at `2.537ms`, `leaf_belief_gather` at `0.359ms`, `leaf_model_writeback` at `0.351ms`, `leaf_showdown_ev` at `0.280ms`, and cached `leaf_feature_assembly` at `0.026ms`, making model value inference the dominant remaining leaf cost.
- BetterFFN now caches its fixed per-hand rank/suit embedding only when gradients are disabled, so CFR leaf inference avoids recomputing the same hand embedding while training forwards still use the normal autograd path. The component run `outputs/cfr_iterator_spots_hand_embedding_cache_components_ab8.json` reduced `leaf_model_forward` from `2.537ms` to `2.427ms` and `cfr_iteration` to `8.274ms`; the repeat full run `outputs/cfr_iterator_spots_hand_embedding_cache_repeat_ab20.json` measured `set_leaf_values` at `3.115ms` and `cfr_iteration` at `8.312ms`, but segment wall remained noisy (`11.215ms` pre-delay, `10.151ms` post-delay), so the overall `<=10.0ms/iter` target is still not closed.
- BetterFFN leaf evaluation can now skip model-side zero-sum reduction when `cfr_avg=false`; the fused model-value writeback applies the same zero-sum correction while scattering into `latest_values`. `outputs/cfr_iterator_spots_fused_zero_sum_repeat_ab20.json` reduced `leaf_model_forward` to `2.065ms`, `leaf_model_writeback` to `0.285ms`, `set_leaf_values` to `3.044ms`, and `cfr_iteration` to `8.188ms`. The latest confirmation run with the explicit compile-mode option plumbing, `outputs/cfr_iterator_spots_zero_sum_compile_option_final_ab20.json`, measured `leaf_model_forward` at `2.057ms`, `set_leaf_values` at `3.066ms`, and `cfr_iteration` at `8.170ms`; segment wall was `11.160ms` pre-delay and `9.298ms` post-delay, so the fused-to-fused wall target remains open.
- `torch.compile` mode is now a first-class config and benchmark option via `model.compile_mode` and `scripts/bench_cfr_iterator_spots.py --compile-mode ...`. The legacy `P2_FUSED_COMPILE_MODE` environment override still works for the fused evaluator. A short smoke run, `outputs/cfr_iterator_spots_max_autotune_option_smoke.json`, confirms `--compile-mode max-autotune` runs end to end; the smoke timing includes first-use autotune/compile overhead and is not used as core-loop evidence.
- The leaf writeback component benchmark now times the actual zero-sum writeback path used by `cfr_avg=false`. With that corrected timer, changing the no-mix zero-sum writeback launch from 8 warps to 4 warps reduced `leaf_model_writeback` from `0.420ms` in `outputs/cfr_iterator_spots_actual_zero_sum_writeback_components_ab4.json` to `0.409ms` in `outputs/cfr_iterator_spots_zero_sum_writeback_4warps_components_ab4.json`. The longer realistic run `outputs/cfr_iterator_spots_zero_sum_writeback_4warps_ab20.json` measured `10.015ms/iter` (`10.670ms` pre-delay, `9.360ms` post-delay), and the repeat without isolated component benches measured `10.127ms/iter`; this is very close but still not conclusive enough to mark the `<=10.0ms/iter` objective complete.
- The spots iterator harness now reports `regret_matching_parent_sum_divide`, the regret-matching parent-sum/divide substep inside `update_policy`. `outputs/cfr_iterator_spots_regret_matching_parent_sum_divide_components_ab2.json` measured it at `0.709ms` CUDA, compared with `update_policy` at `2.628ms`. Two launch-shape probes were rejected: `BLOCK_H=256` regressed `update_policy` to `2.637ms`, and 8 warps left the isolated timer essentially unchanged at `0.707ms`.
- The harness also reports `reach_beliefs_avg_fused`, the fused reach propagation, belief normalization, and average-policy accumulation depth sweep used by the current stat-free hot path. `outputs/cfr_iterator_spots_reach_beliefs_avg_fused_components_ab2.json` measured it at `1.686ms` CUDA, making it the largest remaining `update_policy` subcomponent. A 4-warp launch probe regressed this timer to `1.831ms` in `outputs/cfr_iterator_spots_reach_beliefs_avg_4warps_components_ab2.json`, so the 8-warp launch remains in place.
