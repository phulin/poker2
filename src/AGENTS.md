## Directory summary
This is the Python source root. Setuptools discovers packages from here via `pyproject.toml`.

### Source files
There are no direct Python modules in this directory.

### Subdirectories
- `p2/`: Main HUNL training, environment, model, RL, and CFR/search package.
